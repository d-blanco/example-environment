apiVersion: argocd-image-updater.argoproj.io/v1alpha1
kind: ImageUpdater
metadata:
  name: example-application-updater
spec:
  namespace: argocd
  commonUpdateSettings:
    updateStrategy: "semver"
  writeBackConfig:
    method: "git"
    gitConfig:
      repository: "git@github.com:myorg/myrepo.git"
      branch: "main"
      writeBackTarget: "kustomize:./applications/example-application"   # folder containing kustomization.yaml
  applicationRefs:
    - namePattern: "example-application"
      images:
        - alias: "api"
          imageName: "docker.io/danblanco/example-application:v4.x"
